# LLM Configuration for Cognifs
# This file configures the LLM provider used for intelligent tag generation

provider: local
model_path: ~/.local/share/models/guff/model.bin
executable: guff

# Future configuration for remote providers:
# provider: openai
# api_key: ${OPENAI_API_KEY}
# model: gpt-3.5-turbo

